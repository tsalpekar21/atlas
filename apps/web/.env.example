# Public base URL of the API (Better Auth client + fetch). No trailing slash.
VITE_API_URL=http://localhost:4111

# Optional. Origin for absolute patient triage demo links. Leave unset for same-origin relative URLs.
# VITE_PATIENT_TRIAGE_ORIGIN=http://localhost:3000
